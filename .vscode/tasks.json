{
    "tasks": [
        {
            "type": "shell",
            "label": "cmake and make",
            "command": "cmake .. && make",
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        },
    ],
    "version": "2.0.0"
}